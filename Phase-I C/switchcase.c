#include<stdio.h>
int main(){
    int choice = 1;
    switch (choice)
    {
        case 1:
            /* code */
            printf("1 is chosen");
        case 2:
            printf("2 is chosen");
        default:
            printf("Default");
        
    }
}
